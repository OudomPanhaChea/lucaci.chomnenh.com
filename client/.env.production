# Baked into the client bundle by `next build` (production builds only).
# hPanel env vars take precedence if the build environment provides them;
# this file guarantees the socket connects to the API origin even if not.
NEXT_PUBLIC_API_ORIGIN=https://api-lucaci.chomnenh.com
NEXT_PUBLIC_SITE_URL=https://lucaci.chomnenh.com
